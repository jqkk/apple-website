import styled from '@emotion/styled';
import { Headline, SubHead } from '../@common.styled';

export const StyledHeadline = styled(Headline)`
  color: #ffffff;
`;

export const StyledSubHead = styled(SubHead)`
  color: #ffffff;
`;
